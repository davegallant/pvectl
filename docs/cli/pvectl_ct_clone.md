## pvectl ct clone

Clone a container

```
pvectl ct clone <name-or-vmid> [flags]
```

### Options

```
      --description string   description for the clone
      --full                 force a full (independent) clone instead of a linked clone
  -h, --help                 help for clone
      --hostname string      hostname for the clone (defaults to the source's)
      --newid int            vmid for the clone (0 = auto-assign the next free ID)
      --pool string          resource pool for the clone
      --snapname string      clone from this snapshot instead of the container's current state
      --storage string       target storage for a full clone (defaults to the source's storage)
      --target string        node to create the clone on (defaults to the source's node)
```

### Options inherited from parent commands

```
      --debug           log Proxmox API request/response activity to stderr
  -o, --output string   output format: "table" or "json" (default "table")
      --verbose         show Proxmox task IDs (UPIDs) alongside action output
```

### SEE ALSO

* [pvectl ct](pvectl_ct.md)	 - Manage containers

###### Auto generated by spf13/cobra on 21-Jul-2026
