## pvectl ct backups restore

Restore a container from a backup

```
pvectl ct backups restore [name-or-vmid] [flags]
```

### Options

```
  -h, --help             help for restore
      --node string      restore from any backup on this node instead of an existing container's own backups (disaster recovery — cannot be combined with a name-or-vmid argument)
      --storage string   target storage for the restored container (skips the interactive prompt when set)
      --vmid int         target vmid for --node mode (default: the vmid recorded in the chosen backup)
      --volid string     backup volid to restore (skips the interactive listing/prompt when set)
  -y, --yes              skip the confirmation prompt
```

### Options inherited from parent commands

```
      --debug     log Proxmox API request/response activity to stderr
      --verbose   show Proxmox task IDs (UPIDs) alongside action output
```

### SEE ALSO

* [pvectl ct backups](pvectl_ct_backups.md)	 - Manage a container's backups

###### Auto generated by spf13/cobra on 18-Jul-2026
