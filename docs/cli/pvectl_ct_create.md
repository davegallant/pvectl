## pvectl ct create

Create a new LXC container

```
pvectl ct create [flags]
```

### Options

```
      --arch string                  container architecture (default "amd64")
      --cores int                    CPU cores (default 1)
      --disk-size int                root filesystem disk size in GB (default 8)
      --features string              container features, e.g. nesting=1 (default "nesting=1")
  -h, --help                         help for create
      --hostname string              container hostname (prompts if omitted)
      --memory int                   memory in MB (default 512)
      --net0 string                  network interface config (Proxmox net0 syntax) (default "name=eth0,bridge=vmbr0,ip=dhcp")
      --node string                  node to create the container on (prompts if omitted)
      --password string              root password (optional; omit along with --ssh-public-key-file for console-only access)
      --ssh-public-key-file string   path to an SSH public key file to authorize for root (optional)
      --start                        start the container after creating it (prompts if omitted)
      --storage string               storage for the container's root filesystem (prompts if omitted)
      --swap int                     swap in MB (default 512)
      --template string              OS template volid, e.g. local:vztmpl/ubuntu-24.04-standard_24.04-2_amd64.tar.zst (prompts if omitted)
      --unprivileged                 create an unprivileged container (default true)
      --vmid int                     container ID (0 = auto-assign the next free ID)
```

### Options inherited from parent commands

```
      --debug     log Proxmox API request/response activity to stderr
      --verbose   show Proxmox task IDs (UPIDs) alongside action output
```

### SEE ALSO

* [pvectl ct](pvectl_ct.md)	 - Manage containers

###### Auto generated by spf13/cobra on 21-Jul-2026
